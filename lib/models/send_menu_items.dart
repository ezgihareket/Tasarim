import 'package:flutter/material.dart';

class SendMenuItems{
  String text;
  MaterialColor color;
  IconData icons;

  SendMenuItems({@required this.text,@required this.color, @required this.icons});
}